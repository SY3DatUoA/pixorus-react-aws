version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - chmod +x node_modules/.bin/react-scripts
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
